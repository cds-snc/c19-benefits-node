{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('404') }}</h1>
    <div data-gc-analytics-error="404">
        <p>{{ __('Page not found') }}</p>
    </div>

    {% if message %}
        <pre class="error">{{ message }}</pre>
    {% endif %}

{% endblock %}