{% extends "base.njk" %}
{% from 'banner.njk' import banner as banner %}

{% block content %}
    <h1>{{ __('start.title') }}</h1>

    <div><p>{{ __('start.intro') | safe }}</p></div>

    <h2 class="text-2xl md:mt-12 mb-5">{{ __('start.subtitle') }}</h2>
    <div>
        <a class="button-link" href="" role="button" draggable="false">
            {{ __('start.submit') }}
        </a>
    </div>

{% endblock %}
