{% extends "base.njk" %}

{% block content %}
    <div>
        {% include "../../views/_includes/header-link-question.njk" %}
        <form method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            
            {{ radioButtons('applied_to_EI', 
            [ 
                { value: '1', text:__('applied_to_EI.1')},
                { value: '2', text:__('applied_to_EI.2')},
                { value: '3', text:__('applied_to_EI.3')}
            ], 
            data.applied_to_EI, 
            __('form.applied_to_EI'), 
            errors, 
            { required: true }) 
        }}

            {{ formButtons() }}
        </form>
    </div>
{% endblock %}
